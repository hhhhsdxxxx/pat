#include <iostream>

using namespace std;

int main(int argc, char const *argv[])
{
	int N, M, c1, c2;
	cin >> N >> M >> c1 >> c2;
	int *survive = new int[N];
	for(int i = 0; i < N; i++)
		cin >> survive[i];
	int **road = new int[N]
;	return 0;
}