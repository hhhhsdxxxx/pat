#include <iostream>
#include <vector>

using namespace std;

struct oil_station{
	double p, d;
	oil_station(double pp, double dd):p(pp),d(dd){}
};

vector<oil_station> station;
double maxcap, dis, consume, sum = 0.0;

bool next(int pos, double oil_left){

}

int main(){
	int N;
	cin >> maxcap >> dis >> consume >> N;
	while(N--){

	}
	return 0;
}